Gulp-tslint type definition errors
----------------------------------

This is an example project to demonstrate errors with type definitions for gulp-tslint. Run `npm run test` or open the gulpfile.ts with VS Code to 
see the problem.
